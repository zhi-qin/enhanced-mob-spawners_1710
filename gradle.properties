# Mod Info
modName = Enhanced Mob Spawners
modId = spawnermod
modGroup = com.branders.spawnermod
modVersion = 1.0.0

# Minecraft & Forge versions
minecraftVersion = 1.7.10
forgeVersion = 10.13.4.1614

# MCP mappings
channel = stable
mappingsVersion = 12
remoteMappings = https\://raw.githubusercontent.com/MinecraftForge/FML/1.7.10/conf/

# Dev environment
developmentEnvironmentUserName = Dev

# Disable Mixins (not needed for this mod)
usesMixins = false

# Proxy settings (use your local proxy)
systemProp.http.proxyHost=127.0.0.1
systemProp.http.proxyPort=7897
systemProp.https.proxyHost=127.0.0.1
systemProp.https.proxyPort=7897

# Per-project Java isolation: 1.7.10 工具链强制要求 Gradle 运行在 JDK 8 上。
# 本机用户级 gradle.properties 默认把 Gradle daemon 指向 JDK 25（供 1.21.1 Fabric 项目使用），
# 这里用项目级配置覆盖为 JDK 8（项目级 gradle.properties 优先级高于用户级）。
# 换机器时按实际情况修改此路径。
org.gradle.java.home=C:/Users/25123/.jdks/jdk8u502-b07
